设置 /etc/profile

```
export proxy="http://192.168.202.59"
export http_proxy=$proxy
export https_proxy=$proxy
export ftp_proxy=$proxy
export no_proxy="localhost, 127.0.0.1, ::1"

source /etc/profile
```

